#!/bin/bash
#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints a string without using printf()
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	Return (0)
}
