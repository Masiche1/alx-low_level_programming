#include <stdio.h>

/**
 * main - Entry point
 * Description: Prints numbers to base 10 starting with 0
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 0; a < 0; a++)	
		printf("%d", a);
	printf("\n");
	return (0);
}
